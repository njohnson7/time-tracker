<!DOCTYPE html>
<html>
<head>
  <title>Time Tracker</title>
  <link rel='stylesheet' href='/stylesheets/time_tracker.css'>
</head>

<body>

  <header class='primary-header container group'>
    <h1 class='logo'>
      <a href='/'>Time Tracker</a>
    </h1>

    <h5 class='time'><%= Time.now %></h5>

    <nav class='nav'>
      <ul>
        <li><a href='/'>Activities</a></li>
        <li><a href='/new'>New</a></li>
        <li><a href='#'>Sign in</a></li>
      </ul>
    </nav>
  </header>

  <section>
    <% if session[:msg] %>
      <div class='msg'>
        <%= session.delete(:msg) %>
      </div>
    <% else %>
      <div class='no-msg'>
        &#8196
      </div>
    <% end %>
  </section>

  <%= yield %>

</body>
</html>
